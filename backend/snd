#!/usr/bin/env python3
"""Executable entry point for the snd CLI (push full DAW projects).

    ./backend/snd push ~/Projects/Neon --project "Neon Warehouse" --message "v12"
"""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from snd_cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
